[pytest]
minversion = 8.0
testpaths = tests
asyncio_mode = auto
pythonpath = src
addopts =
    --cov=app
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=70
    -ra
filterwarnings =
    ignore::DeprecationWarning
